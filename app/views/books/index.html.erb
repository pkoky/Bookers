<p id="notice"></p>
<h1>Book</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
    </tr
  <% end %>
  </tbody>
</table>

<div class="sidebar">
  <a 
</div>
  
  <h2>New Book</h2>
<%= form_with model:@book,local:true do |f| %>

<div class="fielld">
  <label for="book_totle">Title</label>
  <%= f.text_field :title %>
</div>

<div class="field">
  <label for="book_body">Body</label>
  <%= f.text_area :body %>
</div>

<div class="actions">
  <%= f.submit "Create Book" %>
</div>

<% end %>